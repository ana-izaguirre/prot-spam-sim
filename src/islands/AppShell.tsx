/**
 * Hydration entry point for the ProtSpam HPC Suite.
 *
 * Astro mounts this component as the single React island of the site; it owns
 * the language/theme provider that every module reads from.
 */
export { default } from '../App';
